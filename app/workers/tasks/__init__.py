"""Arq task implementations — one file per concern.

Added as phases progress:
    Phase 2  → notifications dispatcher
    Phase 4  → images (resize, EXIF strip)
    Phase 6  → stats (nightly aggregation)
    Phase 7  → sms (Eskiz/Playmobile), push (FCM)
"""
